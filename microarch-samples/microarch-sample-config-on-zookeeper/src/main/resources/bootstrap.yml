spring:
  application:
    name: demo-ConfigOnZK-service
  cloud:
    zookeeper:
      config:
        enabled: true
        root: microarch-configuration
        defaultContext: defaultSetting
        profileSeparator: '::'